# CLAUDE.md — 入社書類作成アプリ

このリポジトリは株式会社資さん人事採用課の入社手続きツール。
2026-07-13の引継ぎ書(別セッション「週報メーカー開発」より)の内容を正として取り込み済み。

## 大元ファイル(単一の情報源)

- 新入社員一覧(Googleスプレッドシート):
  https://docs.google.com/spreadsheets/d/1v4w-kRFmN49vt5dQE5dj2zhoJJOPGvQUPOxm6L7tMdA/edit
- Google Drive連携で直接読める(fileId: `1v4w-kRFmN49vt5dQE5dj2zhoJJOPGvQUPOxm6L7tMdA`)。
  **ユーザーにダウンロードを頼まない**(アプリはURL読み込みとxlsxアップロード両対応)
- 2行目がヘッダー、3行目以降がデータ。日程列は数式(Googleエクスポートは計算結果込み)
- 給与テーブル(等級⇔月給の正解表)は templates/keiyakusho.xlsx の「給与テーブル」シート

## 鉄則(仕様に昇格した確定事項)

1. **入社辞令はゼロから作り直すのは絶対NG**(金の飾り枠・HG正楷書体-PRO・ルビが失われる)。
   `templates/jirei_original.doc`(繁浪由香さん・令和8年7月6日付の原本)の
   **等長バイト置換のみ**(generator/jirei.py)。レイアウト情報には一切触れない。
   - ふりがなはEQフィールド2つ。長さの余りは hps32 の後のスペースで調整
   - フィールド境界が動く場合はTable stream内のフィールドPLCのCP5つ
     (0, F1末, F2先頭, F2末, 254)を更新
   - 日付: 8文字は等長置換 / 9文字は本文の読点1つ+日付行の先頭全角空白で吸収 /
     10文字(10月15日型)は未対応=手動
   - 検証必須: LibreOfficeでdocx変換し ①ルビ2組 ②日付4回 ③旧氏名・旧日付の残存なし
   - PDF化はWord限定(LibreOfficeは原本のテキストボックス・飾り枠を描画できない)
   - ファイル名: `YYYYMMDD_入社辞令(氏名).doc`(YYYYMMDD=作成日)
2. **原本があるものは原本を生かす**。レイアウト・書字方向・書体を勝手に変えない。
   内部データからの推測より、ユーザーが知っている実物の見た目が正。
3. **社宅段落の確定文面**(generator/defaults.py)。「社宅のご利用をご希望の場合は、」という
   表現は使わない(転居入社では社宅利用が前提)。転居列が「〇」の人にだけ挿入、対象外は段落ごと削除。
4. **メールの送信ボタンは必ず人間が押す**(アプリは下書き.emlまで。誤送信防止)。
5. 雇用契約書のExcelから**給与テーブルシートを必ず削除**してから本人に渡す(全社員の給与一覧のため)。
6. シートで**グレーアウト(背景/文字色グレー/取り消し線)された行=対応済み**。
   入社日が過ぎた行も自動でチェックを外す。
7. **等級・月給チェックは毎回自動実行**し、結果(全員一致か不一致か)を画面に表示。
   ユーザーへの報告時も突合結果を明示する。
8. **メールの宛先ルール**(混同しやすいので注意):
   - **入社案内メール**: To=新入社員本人、**CC=yuki.ono / mako.kobayashi @sukesan.co.jp**(人事2名)。
   - **ジョブカン案内メール**: **To=古川さん本人(kazuyuki.furukawa)、CC=人事3名**
     (fukiko.takahashi / yuki.ono / mako.kobayashi @sukesan.co.jp)、
     **BCC=同じ入社日の新入社員全員**(自動設定)。本文冒頭に送信前チェックを必ず入れる。
   - ジョブカンは入社月の前月25日(土日は直近平日へ前倒し)にアカウント付与メールを別システムが自動送信。
     その**1週間前に事前案内、当日に登録日リマインド**を送る。日付は自動計算・自動記入。
     添付「入社時のワークフロー申請について」= templates/jobkan_workflow.pdf(差し替え可、自動添付)。
     送信忘れ防止に、送信予定日をカレンダー(.ics)へ登録できる機能をアプリに用意。
9. **フールプルーフ優先**。起動=デスクトップのアイコン1つ(start.batが自動作成)、
   シートは起動時に自動読み込み、普段触らない設定はすべて折りたたむ。
10. **ファイル命名ルールは全書類共通**: `作成日付_ファイル名(氏名様).拡張子`
    (例: `20260716_入社のご案内(伊藤まゆ佳様).pdf`)。日付=実際の作成日(YYYYMMDD)、
    氏名の後ろに「様」を付ける。辞令だけのルールではない。
11. **保存先の構成**: デスクトップの`準備済_入社社員必要書類`フォルダの直下に
    `入社年月_氏名`フォルダ(例: `202608_伊藤まゆ佳`。年月=入社する月)を作り、
    その中に本人の全ファイルを格納。ジョブカンメールは
    `入社年月_ジョブカン案内メール(◯月◯日入社)`フォルダ。
12. **入社案内メールの添付は「入社のご案内」のみ**(+転居〇の方だけ社宅案内・
    マニュアルの2点を追加)。雇用契約書・入社辞令は生成はするがメールに添付しない。
13. **修正を繰り返しても安全な3点セット**: ①再生成時は既存書類を上書きせず
    「旧版」サブフォルダへ退避 ②メール添付は同種書類の最新日付のみ
    ③Word/ExcelがPDFより新しければ画面で警告し、メール作成時は自動で変換し直す。

## 運用ルール

- ユーザーは非エンジニア。手順は番号付きで最小限・専門用語なし
- 成果物はチャットに直接ファイル添付して渡す(配布ZIPは中身を英数字ファイル名にする)
- 迷ったら作る前にユーザーに確認する
- 用途が違うツールは専用リポジトリに分ける
  (週報=fkzyk/weekly-report-maker、スカウト拡張=fkzyk/gourmetcaree、入社手続き=本リポジトリ)
- 指摘・失敗があったら必ず本ファイルの「失敗と教訓ログ」に追記してからコミット

## 失敗と教訓ログ

| 失敗 | 教訓 |
|---|---|
| 辞令をゼロから再現し続けて「原本と違いすぎ」(別セッション) | 原本の等長バイト置換方式に転換して解決(鉄則1が正) |
| .doc内部の縦書き設定を根拠に勝手に縦書き化(別セッション) | 内部データで推測しない。実物の見た目が正。横書きが正解 |
| 明朝体・教科書体で代替し「ゴシックに見える」の指摘(別セッション) | 書体はHG正楷書体-PRO。正式PDFはWordから |
| 枠画像をヘッダーに行内配置→本文が押し下げられ大ズレ(別セッション) | 全面背景はヘッダー内で背面・浮動配置。「PDFにすれば直る」は無い |
| 手動改行を埋め込み行頭がズレた(別セッション) | 手動改行はしない。PDFはWordファイルを変換して作る(単一ソース) |
| 別会話の成果物が見えず行き違い(別セッション) | 会話をまたぐ情報はCLAUDE.md・引継ぎ書に明文化 |
| 本セッションで辞令をpython-docxでゼロから再構築してしまった | 引継ぎ書受領後に鉄則1へ全面切替。旧コードは削除済み |
| 雇用契約書PDFに給与テーブル(全等級の給与一覧)が8ページ分入っていた | 生成時に給与テーブルシートを削除(鉄則5) |
| pyarrow 22以降+Streamlitで画面更新時にセグフォルト | requirementsで `pyarrow<22` に固定(Python3.13以下) |
| Python 3.14にはpyarrow<22のWindows wheelが無くビルド失敗 | start.batがPython 3.12を優先使用・自動インストール |
| 配布ZIPの日本語ファイル名がWindows展開で文字化け | 配布物内のファイル名はすべて英数字にする |
| pywin32の入れ忘れでOffice搭載PCでもPDF変換に失敗 | requirementsに `pywin32; sys_platform == "win32"` |
| バッチ内の日本語コメントがcmd.exe(CP932)で誤動作の恐れ | .batはASCIIのみで書く |
| openpyxlで再保存したxlsxは数式のキャッシュ値が消え日付が読めない | 日程が読めない行は警告して作成をブロック(防御実装済み) |
| 命名ルール「作成日付_ファイル名(氏名)」を辞令だけに適用していた | 命名ルールは全書類共通(鉄則10)。引継ぎの記載は一般ルールとして読む |

## 残タスク(引継ぎ分)

1. ~~ジョブカン案内メールのBCC対象確認~~ → 回答済み: BCC=同じ入社日の新入社員全員。アプリに実装済み
2. ~~等級・月給チェックの実行~~ → アプリが毎回自動実行(2026-07-14時点: 全員一致)
3. ~~辞令の全自動化~~ → 本アプリに実装済み(原本等長置換方式)
4. 入社手続きツール専用リポジトリ → 本リポジトリ(fkzyk/newstaff_file)に集約済み
